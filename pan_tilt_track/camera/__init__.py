"""Camera sources: import from `.source` / `.gstreamer_source` / `.rtsp_stream`
directly. `.rtsp_stream` requires the GstRtspServer gi bindings; kept out of
this package's eager imports since it's optional."""
